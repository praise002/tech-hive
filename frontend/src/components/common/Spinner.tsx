import { FaSpinner } from 'react-icons/fa';

function Spinner() {
  return (
    <div>
      <FaSpinner className="m-auto animate-spin h-5 w-5 text-red" />
    </div>
  );
}

export default Spinner;
